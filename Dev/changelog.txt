# Changelog
CRITICAL NOTE: Must record any changes to code with date and time stamps. NO LOCATION, and be updated after each step is complete before reporting back.
## 2026-06-13

### Base
- Cloned original `Titans-Project/Psycho_Core-8.3.7` into workspace at `Psycho_Core-8.3.7`.
- Base commit: `bba77f6 Merge pull request #39 from Thordekk/main`.

### Toolchain alignment already applied before this changelog
- Set root CMake minimum to `4.3.2`.
- Added/updated Psycho_Core toolchain files:
  - `cmake/macros/FindBoost.cmake`
  - `cmake/macros/FindMySQL.cmake`
  - `cmake/macros/FindOpenSSL.cmake`
  - `dep/boost/CMakeLists.txt`
  - `dep/cotire/CMake/cotire.cmake`
- Aligned OpenSSL/Resolver runtime compatibility files with Psycho_Core:
  - `src/common/Cryptography/OpenSSLCrypto.cpp`
  - `src/common/Cryptography/OpenSSLCrypto.h`
  - `src/common/Asio/Resolver.h`
  - `src/server/bnetserver/REST/LoginRESTService.cpp`
  - `src/server/shared/Realm/RealmList.cpp`
  - `src/server/shared/Realm/RealmList.h`
  - `src/server/worldserver/Main.cpp`
- Updated README toolchain requirements.

### Current session
- Created `changelog.txt`.
- Created `chatlog.txt`.

### Module support port
- Added Psycho_Core module build infrastructure, excluding `mod-psychobot` itself:
  - `cmake/macros/ConfigureModules.cmake`
  - `modules/CMakeLists.txt`
  - `modules/ModulesLoader.cpp.in.cmake`
  - `modules/ModulesLoader.h`
  - `modules/README.md`
- Replaced `cmake/options.cmake` with Psycho_Core version to add `MODULES=none|static|dynamic` support.
- Updated root `CMakeLists.txt` to include `ConfigureModules` and add `modules/` before `src/` when modules are enabled.
- Updated `src/server/worldserver/CMakeLists.txt` to link the `modules` target and depend on dynamic module targets.
- Updated `src/server/game/Scripting/ScriptMgr.cpp` to call `AddModulesScripts()` after static core scripts load.
- Adapted `cmake/macros/FindOpenSSL.cmake` for this Psycho_Core branch so Unix/Linux configure respects `OPENSSL_ROOT_DIR` before falling back to system OpenSSL. This allows local OpenSSL 3.5.6 extraction to be used without replacing system packages.

### Local dependency setup for configure-only test
- Installed CMake 4.3.2 locally for the session under `/tmp/cmake-4.3.2-py` using Python package `cmake==4.3.2`.
- Downloaded and extracted Debian Boost 1.83 packages locally under `/tmp/boost183-root` without system install/root privileges.
- Downloaded and extracted Debian OpenSSL 3.5.6 packages locally under `/tmp/openssl356-root` without system install/root privileges.

### Configure-only verification
- Ran CMake configure only with:
  - `CMake 4.3.2`
  - `Boost 1.83.0` from `/tmp/boost183-root/usr`
  - `OpenSSL 3.5.6` from `/tmp/openssl356-root/usr`
  - `MariaDB client 11.8.6`
  - `SCRIPTS=static`
  - `MODULES=static`
  - `TOOLS=1`
- Configure completed successfully with exit code `0`.
- No build command was run.

### Psycho_Core-specific configure fix
- Adapted `cmake/macros/FindOpenSSL.cmake` so Unix/Linux configure prefers `OPENSSL_ROOT_DIR` include/lib paths before falling back to system OpenSSL. This was required to make local OpenSSL 3.5.6 detection work on Linux.

### Complete module folder import
- Replaced workspace `modules/` with the complete Psycho_Core `modules/` folder.
- `modules/` now includes full `mod-psychobot` source, SQL, config, docs, and loader files.
- Copied module-related docs referenced by `modules/README.md` where available:
  - `docs/HOW_TO_BUILD_A_MODULE.md`
  - `docs/HOW_TO_INSTALL_MODULES.md`
  - `Dev/playerbots_docs/ModuleAPI_Reference.txt`
  - `Dev/playerbots_docs/FUTURE_MOD_PSYCHOBOT_UPGRADES.txt`

### mod-psychobot socketless core hooks
- Added Psycho_Core-compatible `WorldSession` constructor overload for Psycho/mod-psychobot's 11-argument socketless session construction.
- Added `WorldSession::IsBot`, `SetBot`, `SetBotRemove`, `IsBotRemoving`, and `BotLogin` API used by `mod-psychobot`.
- Added bot-safe null-socket handling in `WorldSession::SendPacket`, `WorldSession::Update`, `WorldSession::KickPlayer`, and `World::AddSession_` queue logic.
- Added `WorldSession::BotLogin(ObjectGuid)` in `CharacterHandler.cpp` using the existing `LoginQueryHolder` async login path.

### Research/documentation reports
- Added `MODULE_RESEARCH_REPORT.md` documenting module architecture, docs reviewed, imported module files, mod-psychobot architecture, and integration status.
- Added `API_DIFFERENCE_REPORT.md` documenting relevant Psycho_Core vs Psycho API differences, spell/talent/DB2 risk areas, module include dependencies, and S28 hook presence.

### Final configure-only verification after full modules import and S28 hooks
- Reran CMake configure with full `mod-psychobot` present and S28 socketless hooks added.
- Configure succeeded with exit code `0`.
- CMake detected:
  - `Boost 1.83.0`
  - `OpenSSL 3.5.6`
  - `MariaDB client 11.8.6`
  - `MODULES=static`
  - `mod-psychobot` under module configuration
- No build command was run.

### File completeness verification
- Added `FILE_COMPLETENESS_REPORT.md`.
- Verified workspace Psycho_Core contains all original cloned Psycho_Core files; no original files are missing/deleted.
- Verified workspace `modules/` file inventory against Psycho repo; no module files are missing.
- Verified workspace `modules/mod-psychobot` against Psycho repo; all 159 files are present and byte-identical.

### Corrected file completeness verification
- Re-generated `FILE_COMPLETENESS_REPORT.md` using the Psycho_Core workspace Git index for original tracked file completeness and a fresh Psycho reference clone at `/tmp/Psycho_Core_ref` for module hash verification.
- Verified 0 deleted/missing original tracked Psycho_Core files.
- Verified `modules/` is complete: 163/163 files present and byte-identical to Psycho reference.
- Verified `modules/mod-psychobot` is complete: 159/159 files present and byte-identical to Psycho reference.

### bnetserver build verification
- User permitted exactly one `bnetserver` build.
- Ran one build command targeting `bnetserver` only.
- Result: success, exit code `0`.
- Final build line: `[100%] Built target bnetserver`.
- `worldserver` was not built.
- `modules` / `mod-psychobot` were not built as worldserver-linked targets.

### Non-worldserver build target check
- Inspected generated Makefile targets without building.
- Available non-worldserver targets include `modules`, `mapextractor`, `vmap4extractor`, `vmap4assembler`, `mmaps_generator`, and support libraries.
- Chose `modules` as the most useful next target because it compiles the module library and `mod-psychobot` without building `worldserver`.
- User permitted exactly one non-worldserver build command.
- Ran one build command: `make -C configure-test modules -j2`.
- Result: failed before compilation because generated Makefiles referenced `/tmp/cmake-4.3.2-py/cmake/data/bin/cmake`, which was missing.
- Exit code: `2`.
- Error: `make: /tmp/cmake-4.3.2-py/cmake/data/bin/cmake: No such file or directory`.
- Fix applied after the failed attempt: restored CMake 4.3.2 to `/tmp/cmake-4.3.2-py`.
- No second build command was run.

### Non-worldserver build error fixes
- Fixed core include/API issues found while building non-worldserver targets:
  - Added `MotionMaster.h` include to `Player.h` so `GetMotionMaster()->Initialize()` is usable by external/module code.
  - Added `<cmath>` to `ArenaHelper.h` for `exp`, `expf`, `floor`, `ceil`, `log` under GCC 14.
  - Added `Timer.h` to `BattlegroundPackets.h` for `getMSTime()`.
  - Added `Group::GetMemberFlags(ObjectGuid) const` compatibility helper used by `mod-psychobot`.
  - Added module-target-only `-Wno-narrowing` for GCC/Clang to keep imported module source unchanged while accepting enum-to-uint8 brace initialization.
- Added core race-name compatibility aliases in `RaceMask.h`: `RACE_NIGHT_ELF`, `RACE_UNDEAD`, `RACE_BLOOD_ELF`.

### Non-worldserver build retry and donor suggestion scan
- Reconfigured successfully after core compatibility fixes.
- Re-ran explicit non-worldserver targets: `bnetserver modules scripts mapextractor vmap4extractor vmap4assembler mmaps_generator`.
- Did not build `worldserver`.
- `bnetserver` remained successful.
- `modules` progressed further but stopped in `mod-psychobot` because `PsychobotMgr` has no `SummonBot` member while `mod_psychobot.cpp` calls it.
- Did not edit `mod-psychobot` source.
- Added `DONOR_SUGGESTIONS_REPORT.md` with low/medium/high-risk script and SQL candidates.
- Added missing `PsychobotMgr::SummonBot` declaration/implementation used by `.psychobot summon`.
- Added `<iostream>` to `src/common/Logging/Log.cpp` for non-PCH builds using `std::cout` / `std::cerr`.
- Guarded fallback `localtime_r` declaration/definition behind `_WIN32` so Linux uses the system function in non-PCH builds.
- Added `<ctime>` to `WowTime.h` and `WowTime.cpp` for non-PCH builds.
- Adapted `mod-psychobot` spell lookup to this core: `SpellNameEntry::Name` pointer handling and one-argument `sSpellMgr->GetSpellInfo(spellId)`.
- Fixed `NGrid::SetGridId` const-correctness for GCC/non-PCH builds by removing the invalid `const` qualifier.
- Added `ZoneScript.h` include to `AlliedRaces/allied_races.cpp` for non-PCH scripts build.
- Added `SpellScript.h` include to `BrawlersGuild/brawlers_guild.cpp` for AuraScript definitions in non-PCH scripts build.
- Added `SpellScript.h` include to `BrawlersGuild/brawlers_guild.cpp` for AuraScript definitions in non-PCH scripts build.
- Added non-PCH includes to `BrokenIsles/boss_ana_mouz.cpp`: `ScriptedCreature.h`, `SpellScript.h`, `UnitDefines.h`.
- Added `HealInfo` forward declaration to `SpellScript.h` for non-PCH scripts build.
- Added non-PCH includes to `BrokenIsles/boss_apocron.cpp`: `ScriptedCreature.h`, `SpellScript.h`, `UnitDefines.h`, `MotionMaster.h`.
- Added `Optional.h` include to `MotionMaster.h` for non-PCH builds.
- Added `Optional.h` include to `MotionMaster.h` for non-PCH builds.
- Added `SpellScript.h` and `ScriptedCreature.h` to `ScriptMgr.h` to restore common script include expectations in non-PCH scripts builds.
- Added `InstanceScript` forward declaration to `CreatureAI.h` for non-PCH include order.
- Added `TemporarySummon.h` include to `BrokenIsles/boss_flotsam.cpp` for TempSummon definitions in non-PCH scripts build.
- Added `TemporarySummon.h` to `ScriptMgr.h` shared script include path for non-PCH scripts using `TempSummon`.
- Added `ZoneScript.h` to `ScriptMgr.h` shared script include path for non-PCH zone scripts.
- Removed invalid global `ZoneScript.h` include from `ScriptMgr.h`; added `ZoneScript.h` directly to `BrokenIsles/zone_argus_antoran_wastes.cpp`.
- Added `ZoneScript.h` to all script files deriving from `ZoneScript` for non-PCH zone script builds.
- Added `EventProcessor.h` to `GameObject.h` and `SpellInfo.h` to `ScriptMgr.h` for non-PCH script builds.
- Added `InstanceScript.h` to `ScriptMgr.h` shared script include path for non-PCH encounter scripts.
- Removed invalid global `InstanceScript.h` include from `ScriptMgr.h`; added local `InstanceScript.h` includes to scripts using instance boss-state APIs.
- Added `Creature` forward declaration to `MotionMaster.h` for non-PCH builds.
- Added non-PCH header dependencies: `Errors.h` and `<boost/any.hpp>` to `Chat.h`, `Optional.h` to `CombatAI.h`, and `DB2Structure.h` to `LFGQueue.h`.

### Current non-worldserver build pass
- Added `Position.h` to `CombatAI.h` for `Optional<Position>` in non-PCH builds.
- Added `CreatureAIImpl.h` to all script headers using `GetInstanceAI` for non-PCH builds.
- Added missing script/player/spell/container includes to `BrokenIsles/MicroHolidays/AuctionHouseDanceParty.cpp` for non-PCH scripts build.
- Added `InstanceScript.h` to all scripts using `GetInstanceScript()` for non-PCH builds.
- Added `InstanceScript.h` to scripts using `RegisterInstanceScript` or deriving from `InstanceScript`.
- Added self-contained includes for pet battle headers and Violet Hold assault header: `Position.h`, `DatabaseEnvFwd.h`, and `Player.h` where required.
- Added `Position.h` to `BattlePetPackets.h` for `TaggedPosition` definitions.
- Added missing script/common includes to `Draenor/BloodmaulSlagMines/mob_bloodmaul.cpp` for non-PCH builds.
- Added `<map>` to `GarrisonPackets.h` and `Timer.h` to `Garrison.h` for non-PCH builds.
- Added include guard and required includes to `Draenor/GrimrailDepot/grimrail_depot.h` for non-PCH builds.
- Removed invalid `WorldObject.h` include from `grimrail_depot.h`; `Creature.h` provides the required type in this tree.
- Added `ScriptedCreature.h` to `grimrail_depot.h` for `GetCreatureListWithEntryInGrid` helper.
- Added `CreatureAIImpl.h` to scripts using `RAND(...)` helper for non-PCH builds.
- Added missing includes to `Draenor/IronDocks/iron_docks.hpp` for non-PCH builds.
- Added `Common.h` to `Draenor/ShadowmoonBurialGrounds/shadowmoon_burial_grounds.h` for base integer types.
- Added `<string>` to `ConditionMgr.h`; added `Common.h` and `Optional.h` to `Spell.h` for non-PCH builds.
- Added missing script/spell/unit scheduler includes to `EasternKingdoms/zone_stormwind_city.cpp` for non-PCH builds.

### Current non-worldserver build pass status
- Configured once successfully with low-memory non-PCH flags.
- Built non-worldserver targets only; `worldserver` was not targeted.
- `bnetserver` builds successfully.
- `modules` builds successfully, including `mod-psychobot`.
- `scripts` progressed significantly but is currently blocked by a non-PCH include issue in `EasternKingdoms/BastionOfTwilight/bastion_of_twilight.h` where `Position` is used without including `Position.h`.
- Current blocker: `static const Position Positions[2]` in `bastion_of_twilight.h`.

## 2026-06-14

### Config: database default credentials (research session)
- Edited `src/server/worldserver/worldserver.conf.dist`: changed the 4 DB connection
  defaults from `root;admin;bfa_*` to `Psycho;Psycho;Psycho_*`:
  - LoginDatabaseInfo     = "127.0.0.1;3306;Psycho;Psycho;Psycho_auth"
  - WorldDatabaseInfo     = "127.0.0.1;3306;Psycho;Psycho;Psycho_world"
  - CharacterDatabaseInfo = "127.0.0.1;3306;Psycho;Psycho;Psycho_characters"
  - HotfixDatabaseInfo    = "127.0.0.1;3306;Psycho;Psycho;Psycho_hotfixes"
- Edited `src/server/bnetserver/bnetserver.conf.dist`: LoginDatabaseInfo ->
  "127.0.0.1;3306;Psycho;Psycho;Psycho_auth".
- NOTE: user wrote "Pscyho_auth" (typo); used "Psycho_auth" for consistency.

### CMake: OpenSSL portability
- Edited `cmake/macros/FindOpenSSL.cmake`: added bundled portable search roots FIRST in
  `_OPENSSL_ROOT_PATHS` (both 64/32-bit): `${CMAKE_SOURCE_DIR}/dep/openssl` and
  `${CMAKE_SOURCE_DIR}/dep/openssl/OpenSSL-Win64` (Win32 variant for 32-bit), keeping the
  existing `C:/Program Files/OpenSSL-3_5_6-Win64` etc. fallbacks. Makes source tree portable.
- (Boost FindBoost.cmake + MySQL FindMySQL.cmake already searched dep/ — no edit needed.)

### dep/ install instruction files added (no core code change)
- Added `dep/boost/INSTALL_BOOST.txt` — exact Boost 1.83.0 version, what to unzip into
  dep/boost, and download/compile-from-source fallback instructions.
- Added `dep/openssl/INSTALL_OPENSSL.txt` — exact OpenSSL 3.5.x (3.5.6) Win64 package, what to
  unzip into dep/openssl, and download/install fallback instructions.

### Documentation (Dev/) added this session (no core code change)
- PREFLIGHT_AUDIT_AND_CONFIG_REPORT.md, PORTABLE_SERVER_SETUP_GUIDE.md,
  BUILD_GUIDE_WINDOWS.md, BUILD_GUIDE_LINUX.md, SERVER_QUICKSTART_CHECKLIST.md,
  DEEP_DIVE_Eluna_SD3_Modules_Report.md.

### Research-only items (NO core code changed)
- Downloaded Eluna `tc-retail` branch to `Dev/Eluna-tc-retail/` (204 files, reference only).
- Verified: modules/ left at top-level (move evaluated, declined as risky/no-benefit).
- Verified: ScriptDev3 (MaNGOS, <=MoP) is incompatible with this TrinityCore-based core.

### Code-change scope this session
- ONLY 3 core files edited (all config/cmake): worldserver.conf.dist, bnetserver.conf.dist,
  FindOpenSSL.cmake. No .cpp/.h/scripts/modules source touched. No build run.

### Config: mod-psychobot conf approach (revised per user)
- User wanted module conf at <server root>/conf/. Investigated: this core's ConfigMgr
  (src/common/Configuration/Config.cpp) loads ONLY worldserver.conf (LoadInitial); there is NO
  module-config directory scanner. So a standalone conf/ file would NOT be read.
- Per user decision: MERGED all mod-psychobot keys into worldserver.conf.dist (27 keys) so they
  load natively; repurposed the build's module-conf drop as a RESTORE BACKUP.
- Edited `src/server/worldserver/worldserver.conf.dist`: appended a MOD-PSYCHOBOT block with all
  27 Psychobot.* keys (Enable, FollowDistance, RandomBots.*, ActiveAlone*, SmartScale*,
  DisabledWithoutRealPlayer/LoginDelay/LogoutDelay, Gear*, AhBot.*).
- Edited `cmake/macros/ConfigureModules.cmake`: CopyModuleConfig now drops module *.conf.dist to
  <server root>/conf-backup/ (runtime bin/<cfg>/conf-backup + install conf-backup) instead of
  configs/modules/. Reference/restore copy only.
- Edited `modules/CMakeLists.txt`: updated the conf-copy comment + status message to "-> conf-backup".
- Edited `modules/mod-psychobot/conf/mod_psychobot.conf.dist`: header note now states keys are
  merged into worldserver.conf and this file is a conf-backup restore reference.
- NOTE: during this edit two files were briefly mangled by a sed replace and were RESTORED from a
  fresh upstream clone, then re-edited cleanly with the file editor. Final state verified clean
  (modules/CMakeLists.txt 221 lines, conf.dist 86 lines).

### Updated session code-change scope (config/cmake only; no C++ source, no build)
1. src/server/worldserver/worldserver.conf.dist  (DB defaults + merged psychobot keys)
2. src/server/bnetserver/bnetserver.conf.dist     (DB default)
3. cmake/macros/FindOpenSSL.cmake                 (dep/openssl portable search)
4. cmake/macros/ConfigureModules.cmake            (module conf -> conf-backup)
5. modules/CMakeLists.txt                          (comment/message wording)
6. modules/mod-psychobot/conf/mod_psychobot.conf.dist (header note)
Plus docs: dep/boost/INSTALL_BOOST.txt, dep/openssl/INSTALL_OPENSSL.txt, Dev guides.

### Final verification pass (2026-06-14)
- Verified all 6 intended config/cmake edits intact and clean (content checks [1]-[9] passed):
  1. worldserver.conf.dist  : 4 DB lines = Psycho;Psycho;Psycho_* ; 27 Psychobot.* keys merged.
  2. bnetserver.conf.dist   : LoginDatabaseInfo = Psycho;Psycho;Psycho_auth.
  3. FindOpenSSL.cmake      : dep/openssl portable roots present (6 refs).
  4. ConfigureModules.cmake : module conf -> conf-backup (7 refs).
  5. modules/CMakeLists.txt : conf-backup comment/message (221 lines, clean).
  6. mod_psychobot.conf.dist: header note (86 lines, clean).
- Verified dep/boost/INSTALL_BOOST.txt and dep/openssl/INSTALL_OPENSSL.txt present.
- IMPORTANT CLARIFICATION (diff vs fresh upstream clone):
  A diff against a freshly-cloned upstream showed 3 script .cpp files differing
  (TombOfSargeras/tomb_of_sargeras.cpp, VaultOfTheWardens/boss_cordana_felsong.cpp,
  Pandaria/ThroneOfThunder/boss_jin_rokh_breaker.cpp). These are NOT changes from this session.
  They are the PRIOR non-PCH build fixes already recorded earlier in this changelog
  (#include "InstanceScript.h", getVictim()->GetVictim(), GetGUIDLow()->GetGUID().GetCounter(),
  EMOTE_STATE field access). The upstream repo HEAD has also advanced (8f0701c -> f891117),
  so the fresh clone is a different commit; the delta reflects upstream movement + prior fixes,
  not this session's work. This session touched ONLY the 6 config/cmake files above.
- Snapshot note: Dev/Eluna-tc-retail lost its 4 .github/workflows files to a snapshot drop;
  re-restored from upstream -> 204/204 files complete again.
- No build run this session.

### Documentation added (2026-06-14)
- Added Dev/REBUILD_BUILD_INFO_GUIDE.md: how to rebuild a missing .build.info for an 8.3.7.35662
  client (TOOL method via CASCExplorer + MANUAL method reading BuildConfig/CDNConfig hashes from
  Data/config). Includes the complete-vs-streaming-client check and the .build.info field template.
  Research/doc only - no core code changed.

### Documentation added (2026-06-15)
- Added Dev/CLIENT_DATA_SOURCES_REPORT.md: comprehensive hunt for BFA 8.3.7.35662 client + data
  sources (pre-extracted data packs, full clients, mini clients, repacks, torrents, Discord re-up
  routes). Key find: pre-extracted ClientData 3.73GB pack referenced on LIVE GitHub repo
  CrimsonDespair/BFA - sidesteps the streaming-stub extraction failure entirely. Each link tagged
  with verification status (GitHub repos verified live; file-host links listed but not byte-verified).
  Research/doc only - no core code changed, no build.

### Documentation updated (2026-06-15) - torrent/official-server pass
- Updated Dev/CLIENT_DATA_SOURCES_REPORT.md with a dedicated torrent/archive/official-server sweep.
  Added section 2A (verified-live official-server full client torrents): WoW Circle official BFA
  forum page (58.9GB client torrent, mirrors sendspace.com/file/xhs5wb + wdfiles.ru/4fbee0, plus
  44MB connection-files patch) - page read directly, VERIFIED LIVE; Firestorm official full client
  torrent. Added 5B (live build-matched source repos incl MttAI-dev/MTT-WoW-BfA). Added 6B dead-ends
  (archive.org "full map file" is a wallpaper IMAGE not server data - verified; cloud.bfacore.com
  QNAP likely dead; quota-limited Drive links). Rewrote section 7 recommended order to lead with the
  WoW Circle torrent + pre-extracted pack as two independent verified routes. Refreshed honesty/status
  summary. No core code changed, no build.

### Documentation updated (2026-06-15) - torrent-INDEX deep dive (3rd pass)
- Updated Dev/CLIENT_DATA_SOURCES_REPORT.md with a dedicated torrent-website sweep (new section 5C).
  Searched 1337x, archive.org torrents, getMaNGOS torrent directory, Firestorm magnets directly.
  Findings (all honesty-tagged): WoW Circle 58.9GB official torrent = REAL + reachable (the one to
  use); Firestorm uses real public magnets (10.2.7 magnet confirms format) but BFA magnet is launcher-
  gated; the well-known Sineater213 "BFA + Private Server Emulator" 57GB torrent (16S/47L) appears
  DELISTED from uploader's live 1337x profile, infohash unverifiable -> NO fabricated magnet given
  (uploader proven real via still-live WotLK torrent infohash CB9D7227...). getMaNGOS + archive.org
  (wow_clients 59.7GB) torrent directories are real but contain only Classic/TBC/WotLK/Cata/Turtle -
  no BFA. 1337x live search now returns only BFA soundtracks. Added 3rd-pass update note at top,
  expanded section 8 status summary. No core code changed, no build.

### Documentation updated (2026-06-15) - Boost pre-built pack verified
- Verified user's pre-compiled Boost via `dir`: version 1.83.0 (boost/version.hpp 2023-08-08),
  vc143 (VS2022), x64, static libs, ALL components present (Release+Debug) incl the 6 required
  (system, filesystem, thread, program_options, iostreams, regex) + deps (atomic, chrono,
  date_time, zlib, bzip2). Headers folder confirmed present (version.hpp exists).
- Updated dep/boost/INSTALL_BOOST.txt: added "WHAT THIS PACK SHIPS" section documenting the
  verified pre-built Boost specs + exact zip layout (boost\ + lib64-msvc-14.3\), nesting-check
  warning (dep/boost/boost/version.hpp must exist), leaner Release-only alternative (stage\lib\),
  and warning not to zip bin.v2\. No core code changed, no build.

### Documentation updated (2026-06-15) - Boost two-pack distribution plan
- User decided: ship Release-only Boost pack (boost_dep_release.zip, ~26MB: boost\ + stage\lib\,
  8 release libs) INSIDE dep/boost\ (under GitHub's 100MB in-tree limit); ship the full
  Release+Debug pack (boost_dep.zip, ~280MB: full lib64-msvc-14.3\) as a GitHub RELEASE ASSET
  (allows up to 2GB) for the rare Debug-build case.
- Updated dep/boost/INSTALL_BOOST.txt "WHAT THIS PACK SHIPS" + new "HOW TO INSTALL" section to
  document both packs, the extract-before-cmake step (CMake can't read inside a zip), exact target
  layouts (Release -> dep/boost/stage/lib\; Debug -> dep/boost/lib64-msvc-14.3\), and the
  dep/boost/boost/version.hpp nesting check. No core code changed, no build.
- ACTION ON USER (outside sandbox): place boost_dep_release.zip into dep/boost\; attach
  boost_dep.zip to a GitHub Release.

### Documentation updated (2026-06-15) - Boost release-tab distribution finalized
- User publishing 3 files on GitHub Releases tag V1.0-Boost ("Pre-compiled Boost + EXE"):
  boost_dep_release.zip (26MB, also in repo dep/boost\), boost_dep.zip (280MB), and
  boost_1_83_0-msvc-14.3-64.exe (official installer).
- Rewrote dep/boost/INSTALL_BOOST.txt: documents all 3 packs + the Releases tab (tag V1.0-Boost),
  step-by-step extract-in-place install (A=release zip, B=debug zip, C=official exe), "if you have
  issues" guidance (re-download / use exe / build from source), nesting check, and a cleaned-up
  BUILD BOOST FROM SOURCE fallback (b2 commands for Windows + Linux).
- Updated Dev/BUILD_GUIDE_WINDOWS.md: prereq Boost line + new step 2a (Boost extract workflow)
  pointing at the Releases tab and the version.hpp nesting check.
- Created Dev/RELEASE_V1.0-Boost_description.md: ready-to-paste GitHub release Title/Tag/body +
  file table + install steps + maintainer upload reminder (use the attach box, not the 25MB comment box).
- No core code changed, no build.

### Documentation updated (2026-06-15) - rebranded docs to Psycho-Core only
- Per user request: removed "BfaCore-Reforged" BRANDING from documents; docs now identify as
  "Psycho-Core 8.3.7.35662" only. Scope chosen by user: branding-only; build system left untouched.
- Retitled: BUILD_GUIDE_WINDOWS.md, BUILD_GUIDE_LINUX.md, PORTABLE_SERVER_SETUP_GUIDE.md,
  PREFLIGHT_AUDIT_AND_CONFIG_REPORT.md, REBUILD_BUILD_INFO_GUIDE.md, RELEASE_V1.0-Boost_description.md,
  DEEP_DIVE_Eluna_SD3_Modules_Report.md, dep/boost/INSTALL_BOOST.txt, dep/openssl/INSTALL_OPENSSL.txt.
- Build guide: removed "Titans-Project/BfaCore-Reforged" from clone line; reframed BfaCore.sln note +
  project(BfaCore) preflight row as "upstream build-system name, left as-is so build isn't broken".
- DELIBERATELY KEPT (factual, would break/falsify if renamed): external project/repo names + download
  links in CLIENT_DATA_SOURCES_REPORT.md, and the two comparison reports (psycho_core_restart_assessment.md,
  bfa_sql_assessment.md) which compare Psycho vs the real external BfaCore repos.
- No core/build-system code changed, no build.
