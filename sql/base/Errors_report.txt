===========================================================================
 PSYCHO_CORE 8.3.7 — MASTER ERRORS REPORT
 Maintained: 2026-06-18  |  Updated each session in-place
 Source ZIP: Psycho-Core-8_3_7.zip
===========================================================================

This is the single running report for all audit and fix work on
Psycho_Core 8.3.7. Every session appends to this file — nothing is
deleted. Status markers: [FIXED] [CONFIRMED CLEAN] [NOTE] [PENDING]


===========================================================================
 SESSION 1  (2026-06-10)
 mod-psychobot: PsychobotFactory.cpp/.h initial audit
===========================================================================

[FIXED] #1 — Account index rescan bug (GenerateOneBot)
  GenerateOneBot() rescanned bot accounts from index 1 on every call.
  Fixed: added _nextAccountIndex member to PsychobotFactory.h; loop now
  starts from the cached index and updates it on success.
  File: modules/mod-psychobot/src/PsychobotFactory.cpp/.h

[FIXED] #2 — Orphaned DB rows on purge (PurgeAllBots)
  PurgeAllBots() left rows in ai_playerbot_db_store,
  ai_playerbot_random_bots, and ai_playerbot_guild_tasks after deleting
  bot characters. Fixed: added three DELETE statements keyed on character
  GUID inside the purge loop.
  File: modules/mod-psychobot/src/PsychobotFactory.cpp

[FIXED] #3 — CharacterCache registration gap (GenerateOneBot)
  Freshly spawned bots were not registered with CharacterCache, so
  .psychobot summon/add failed by name until server restart.
  Fixed: call sCharacterCache->AddCharacterCacheEntry() after SaveToDB().
  Added #include "CharacterCache.h".
  File: modules/mod-psychobot/src/PsychobotFactory.cpp

[CONFIRMED CLEAN] S28 WorldSession core edits
  All 7 required hooks verified present and correct:
  WorldSession.h  — m_isBot, m_botRemove, IsBot, SetBot, SetBotRemove,
                    IsBotRemoving, BotLogin (declared)
  WorldSession.cpp — null-socket guards in SendPacket, Update,
                     IsConnectionIdle, KickPlayer
  CharacterHandler.cpp — WorldSession::BotLogin() fully implemented
  No core edits needed.

[NOTE] PsychobotAiFactory.h/.cpp — REQUIRED. Builds each bot's AI
  behavior context (per-class strategies/triggers/values). Not optional.

[NOTE] PvP objective stubs — DoFlagTactics, DoBaseTactics,
  DoResourceTactics, DoArenaTactics always return false. Bots fight in
  BGs/Arenas but don't carry flags or cap bases. Intentional framework
  hooks; not a bug.

[NOTE] RBAC permission 194 comment mislabel — psychobot_rbac.sql
  comments call 194 "the GM role" but 194 = "Role: Sec Level Moderator".
  Functionally correct (193 GM inherits 194 via linked_permissions).
  Comment-only cosmetic issue.

[NOTE] Factory commands run synchronously on world thread — spawning
  50+ bots or purging a large pool will cause a brief world-thread hitch.
  Not a bug; expected TC GM command behavior.


===========================================================================
 SESSION 2  (2026-06-11)
 SQL data review — legacy ai_playerbot_* files
===========================================================================

[CONFIRMED CLEAN] Bot names — psychobot_names.sql already contains both
  ai_playerbot_names and ai_playerbot_guild_names with 69,632+ rows
  (vs uploaded CSV's 10,679). Names are present; do NOT import the
  uploaded CSV files.

[NOTE — DO NOT IMPORT] characters_ai_playerbot_names.sql + .csv
  Three problems: (a) names already exist in psychobot_names.sql with
  more data; (b) LOAD DATA INFILE uses hardcoded Windows path
  'E:/Mangosthree/...'; (c) script deletes names >12 chars and shrinks
  the column — destructive against your existing data.

[NOTE — DO NOT IMPORT] ai_playerbot_guild_names.sql — redundant.
[NOTE — DO NOT IMPORT] characters_ai_playerbot_speech.sql — creates
  ai_playerbot_speech tables; mod-psychobot uses ai_playerbot_texts
  (from psychobot_texts.sql) instead. Speech tables unused.
[NOTE — DO NOT IMPORT] characters_ai_playerbot.sql — random_bots and
  db_store already in module SQL; guild_tasks unused by any source code.
[NOTE — DO NOT IMPORT] characters_ai_playerbot_custom_strategy.sql,
  characters_ai_playerbot_tele_cache.sql, index_world.sql — all
  redundant; already in module SQL.

[NOTE — CONDITIONALLY IMPORT] characters_auctionhousebot.sql +
  ahbot_* indexes from index_characters.sql — ahbot_history/category/
  price tables not in module SQL. PsychobotAhBot.cpp is active and
  ticking. Import ONLY if worldserver logs "Table doesn't exist" errors
  for these tables on startup.


===========================================================================
 SESSION 3  (2026-06-12)
 Full mod-psychobot master-zip audit (Psycho_Core-8.3.7-Master.zip)
===========================================================================

[CONFIRMED CLEAN] Missing files check — all 145 source files present;
  all 12 class folders have complete AiObjectContext/Strategies/Triggers
  trios; all includes resolve; LICENSE.MYCODE.txt present at repo root;
  mod_psychobot_loader.cpp correctly wired; all SQL dirs present.

[CONFIRMED CLEAN] Build-readiness pass — WorldSession 11-arg constructor
  call matches exactly; Player constructor does not call SetPlayer so
  temp session is safe to delete; GetGenerator<HighGuid::Player>() is
  valid; SaveToDB 3-arg form matches; LearnTalent/ActivateTalentGroup
  signatures match; all Allied Race enums resolve via CharacterPackets.h.


===========================================================================
 SESSION 4  (2026-06-13)
 Core swap — Psycho-Core-_8_3_7.zip compatibility check
===========================================================================

[CONFIRMED CLEAN] Module compatibility with new "Reforged + Modernized"
  BFA core — all API calls (talent, spec, group, WorldSession compat
  overload, S28 hooks) verified against this core's specific headers.
  New core already ships a dedicated 11-arg WorldSession overload with a
  comment explicitly naming mod-psychobot.

[CONFIRMED CLEAN] Scripts general — all 88 "Psycho-only/removed" symbols
  checked against all script files: zero hits in any of the 1,338 files.

[FIXED] custom_npcs.cpp — OnQuestAccept(Player*, const Quest quest)
  Parameter declared by value instead of const pointer. Changed to
  Quest const*. compile error / hook never fires without this fix.
  File: src/server/scripts/Custom/custom_npcs.cpp


===========================================================================
 SESSION 5  (2026-06-14)
 Full scripts API audit — all 1,338 script .cpp files
===========================================================================

[FIXED] 167 fixes across 43 script files. Full list:

  Casing errors:
    isAlive->IsAlive (boss_maiden_of_vigilance, 5 lines)
    isInCombat->IsInCombat (5 files)
    isGameMaster->IsGameMaster (boss_cordana_felsong)
    isInTankSpec->GetRoleForGroup()==1 (boss_maiden_of_vigilance)
    getPowerType->GetPowerType (boss_fallen_avatar, boss_harjatan)
    getVictim->GetVictim (boss_cordana_felsong)
    IsLfrRaid->false (boss_kiljaeden x2, boss_maiden_of_vigilance)

  Old Object field write API (removed in BFA):
    SetUInt32Value(UNIT_FIELD_EMOTE_STATE,x)->SetEmoteState(x)
      (boss_sisters_of_moon x3, tomb_of_sargeras x2)
    SetByteValue(GAMEOBJECT_FIELD_BYTES_1,0,0/1)->SetGoState(...)
      (tomb_of_sargeras x4)
    SetInt32Value(UNIT_FIELD_POWER+1,x)->SetPower(GetPowerType(),x)
      (boss_eonar x2)
    GetUInt32Value(UNIT_FIELD_POWER+1)->GetPower(GetPowerType())
      (boss_eonar x3)
    SetUInt32Value(UNIT_VIRTUAL_ITEM_SLOT_DISPLAY*,id)->SetVirtualItem(slot,id)
      (cs_npc.cpp x3)

  Unit/Creature API:
    SetLevitate->SetDisableGravity (boss_twin_valkyr, boss_nightbane)
    SetLootRecipient(null)->ResetLootRecipients() (4 files)
    StopAttack->AttackStop (boss_cordana_felsong x2, boss_sisters_of_moon)
    KillAllDelayedEvents->m_Events.KillAllEvents(false) (2 files)
    HasSpellCooldown->GetSpellHistory()->HasCooldown (boss_fallen_protectors)

  Player API:
    GetSelection->GetSelectedUnit (cs_npc.cpp)
    SetFreeTalentPoints->InitTalentForLevel / commented (3 files)
    CheckQuestStatus->GetQuestStatus==QUEST_STATUS_x (3 files, 36 sites)

  Old packet API:
    SendMonsterMove->MovePoint (halls_of_stone x2)
    BuildHeartBeatMsg->commented (boss_omnotron)

  GUID API:
    GetGUIDLow->GetGUID().GetCounter() (boss_jin_rokh_breaker)

  LFG WoD extensions (not in BFA LFGMgr) -> commented out:
    AutomaticLootAssignation / RewardDungeonDoneFor (17 files)

  Scheduler / delayed actions:
    AddDelayedCombat->GetScheduler().Schedule() (boss_cordana_felsong,
      boss_kiljaeden x5, boss_sisters_of_moon, boss_fallen_avatar x4)
    CastSpellDelay->GetScheduler().Schedule() (boss_elerethe_renferal,
      boss_fallen_avatar)
    MoveSmoothFlyPath->MoveSmoothPath(fly=true) (boss_fallen_avatar)

  Guild/Group:
    IsGuildGroup->false (boss_mannoroth)

[CONFIRMED CLEAN] All remaining hits (DoPermBindPlayersForRaid,
  ForEachPlayer, SendDisplayToast, GetThreatTarget, SetFloatValue on
  AreaTrigger) confirmed inside /* */ comment blocks.

[CONFIRMED CLEAN] mod-psychobot — all 3 PsychobotFactory fixes already
  present in this zip at time of scripts audit.


===========================================================================
 SESSION 6  (2026-06-17)
 Full re-audit — Psycho-Core-8_3_7.zip (latest upload)
===========================================================================

[CONFIRMED CLEAN] mod-psychobot — all 3 PsychobotFactory fixes present.
  _nextAccountIndex, AddCharacterCacheEntry, PurgeAllBots cleanup.

[FIXED] 167 fixes re-applied to this zip's script files (same set as
  Session 5 — fixes had not been carried into this new zip).
  43 files fixed. Zero broken API calls remaining in all 1,338 scripts.
  Full fix list identical to Session 5 above.


===========================================================================
 SESSION 7  (2026-06-18)
 Core files audit — src/server/game/ all .cpp/.h
===========================================================================

[FIXED] #4 — InspectHandler.cpp: Old field API for honor kill stats
  WorldSession::HandleRequestHonorStatsOpcode used removed API:
    player->GetUInt32Value(ACTIVE_PLAYER_FIELD_LIFETIME_HONORABLE_KILLS)
    player->GetUInt16Value(ACTIVE_PLAYER_FIELD_KILLS, ...)
  Neither GetUInt32Value/GetUInt16Value nor those field enums exist in
  this BFA core. Compile error — worldserver would not build.
  Fixed to use typed update-field accessors (consistent with line 73 of
  the same file which already uses the correct form):
    player->m_activePlayerData->LifetimeHonorableKills
    player->m_activePlayerData->YesterdayHonorableKills
    player->m_activePlayerData->TodayHonorableKills
  File: src/server/game/Handlers/InspectHandler.cpp

[CONFIRMED CLEAN] All S28 WorldSession bot hooks (re-verified on this zip):
  WorldSession.h: m_isBot, m_botRemove, IsBot, SetBot, SetBotRemove,
    IsBotRemoving, BotLogin — all present.
  WorldSession.cpp: null-socket guard in SendPacket (line 260),
    bot lifecycle in Update() (lines 512-523), IsConnectionIdle guard
    (line 353), KickPlayer early-return (line 685) — all correct.
  CharacterHandler.cpp:916 — WorldSession::BotLogin() fully implemented.

[CONFIRMED CLEAN] Config keys — all 27 Psychobot.* keys in
  mod_psychobot.conf.dist match exactly with source code usage.

[CONFIRMED CLEAN] CMake — CollectSourceFiles() (GLOB-based) auto-collects
  all 369 core .cpp files. mod-psychobot.cmake is a minimal hook; module
  build engine auto-collects all module sources.

[CONFIRMED CLEAN] RBAC — psychobot_rbac.sql grants permissions 1200-1215
  to role 194 (Moderator) using INSERT IGNORE. Role 193 (GM) inherits
  role 194 via rbac_linked_permissions (193->194 confirmed in 1_auth.sql).

[CONFIRMED CLEAN] Database — LOGIN_GET_ACCOUNT_ID_BY_USERNAME present in
  LoginDatabase.h. PQuery/PExecute exist on DatabaseWorkerPool.
  AccountMgr::CreateAccount, Player::Create, CharacterCache::
  AddCharacterCacheEntry signatures all match PsychobotFactory usage.

[CONFIRMED CLEAN] Remaining old-API hits in core — 5 hits found after
  InspectHandler fix; all confirmed inside /* */ comment blocks:
  BattlePayMgr.cpp, Player.cpp, Guild.cpp, Battleground.cpp,
  ArchaeologyMgr.cpp. None affect compilation.

[NOTE] 182 TODO/FIXME markers in core — all in feature stubs (BattlePets,
  BattlePay, Archaeology, AuctionHouse). Pre-existing; not regressions.
  None affect bot functionality or general worldserver runtime.


===========================================================================
 CURRENT STATUS SUMMARY
===========================================================================

  mod-psychobot module:   CLEAN (all 3 factory fixes applied)
  Core scripts (1,338):   CLEAN (167 fixes applied, 0 broken calls)
  Core files (369 cpp):   CLEAN (1 fix applied — InspectHandler)
  S28 bot session hooks:  CLEAN (all 7 hooks verified)
  Config / SQL / CMake:   CLEAN (all verified)

  Files to apply to your Psycho-Core-8_3_7.zip:
    src/server/scripts/*  — Scripts-FIXED.zip (43 files)
    src/server/game/Handlers/InspectHandler.cpp — Core-Files-FIXED.zip
    modules/mod-psychobot/ — mod-psychobot-FIXED.zip

===========================================================================


===========================================================================
 SESSION 8  (2026-06-19)
 SQL database files — all-DBs.zip / DB-3.zip / DB-4.zip
 (note: "all-DBs.zip" did not actually upload — DB-3.zip and DB-4.zip did)
===========================================================================

YOUR DB CONFIG (from worldserver.conf, used to fix every file):
  LoginDatabaseInfo     = 127.0.0.1;3307;psycho;psycho;psycho_auth
  WorldDatabaseInfo     = 127.0.0.1;3307;psycho;psycho;psycho_world
  CharacterDatabaseInfo = 127.0.0.1;3307;psycho;psycho;psycho_characters
  HotfixDatabaseInfo    = 127.0.0.1;3307;psycho;psycho;psycho_hotfixes

WHAT WAS IN THE TWO ZIPS:
  DB-3.zip  — a single 4_world.sql (583MB). Starts with DROP DATABASE,
              same 245 tables/same row counts as DB-4's world.sql, just
              different INSERT formatting/line endings. OLDER dump.
  DB-4.zip  — 1_auth.sql, 2_characters.sql, 4_world.sql (the 3 base
              schemas) + updates/{world,characters,hotfixes}/ (32 patch
              files). 1_auth.sql and 2_characters.sql are byte-identical
              to the core's own sql/base/ files.

[NOTE] DB-3.zip was NOT used and should NOT be imported.
  Reason: it opens with `DROP DATABASE IF EXISTS` + recreate, and its
  245 tables hold the exact same row counts as DB-4's world.sql — it's
  simply an older/differently-formatted export of the same data, with no
  unique content. Importing it would be pure data loss risk for no gain.

[FIXED] Database name mismatches — all 3 base files plus all 31 update
  files referenced the core's original example DB names (auth_bfa,
  characters_bfa, world_bfa) instead of your actual names. Every
  CREATE DATABASE, USE, and 2 hardcoded inline references
  (`world_bfa`.`trainer_spell` in 2022_01_24_world_fix_trainers.sql)
  were rewritten to psycho_auth / psycho_characters / psycho_world.

[FIXED] MariaDB 11.8.6 strict-mode zero-date rejection — 1_auth.sql has
  legacy TIMESTAMP columns defaulting to '0000-00-00 00:00:00'; world.sql
  has 15 similar zero-date values. MariaDB 11.x's default strict sql_mode
  includes NO_ZERO_DATE/NO_ZERO_IN_DATE, which would reject these inserts
  outright. Fixed by wrapping each file's import with:
    SET SESSION sql_mode = REPLACE(REPLACE(@@SESSION.sql_mode,
      'NO_ZERO_DATE',''), 'NO_ZERO_IN_DATE','');
  ... and restoring the original sql_mode at the end of the file.
  (characters.sql had zero zero-dates — no fix needed there.)

[FIXED] CRLF line endings — DB-4's 1_auth.sql and 2_characters.sql used
  Windows CRLF throughout; normalized to LF for clean MariaDB import.

[FIXED] Update-file application order — confirmed by reading
  src/server/database/Updater/UpdateFetcher.h directly: the core's own
  DBUpdater enumerates update files into a std::set<...,PathCompare>,
  i.e. plain lexicographic filename sort (same as `ls | sort`). All 29
  world/, 2 characters/, and 1 hotfixes/ update files were concatenated
  onto their respective base files in that exact order, so the
  consolidated files behave identically to what your worldserver's own
  auto-updater would have produced.

[FIXED] mod-psychobot SQL folded in — appended to the matching
  consolidated file in deterministic (alphabetical) order:
    auth:       psychobot_rbac.sql
    characters: psychobot_custom_strategy.sql, psychobot_db_store.sql,
                psychobot_random_bots.sql, psychobot_strategies.sql
    world:      psychobot_cache.sql, psychobot_indexes.sql,
                psychobot_names.sql, psychobot_rpg_races.sql,
                psychobot_texts.sql

[NOTE — GAP, NOT FIXED] No base schema for psycho_hotfixes anywhere.
  Neither DB-3.zip, DB-4.zip, nor the core's own sql/base/ contains a
  CREATE-TABLE schema for the hotfixes database. HotfixDatabase.cpp
  shows this core expects a large DB2-mirroring schema (achievement,
  achievement_locale, animation_data, adventure_journal, ... one table
  per client DB2 file) — hundreds of tables that must match the
  prepared statements in HotfixDatabase.cpp exactly. The only hotfixes
  file provided (2022_02_02_hotfixes_fix_fishing_spell.sql) is an UPDATE
  that assumes this schema already exists (it does DELETE/INSERT into
  hotfix_data and spell_effect). I have NOT fabricated this schema —
  guessing at hundreds of table definitions that must byte-match C++
  prepared statements is too high-risk to invent. You'll need the
  matching hotfixes base SQL from wherever you sourced DB-3/DB-4 (it's
  normally a separate "hotfixes" or "db_hotfixes" dump), or it can be
  generated by running this core's DB2 hotfix-extractor tool against
  your client's DB2 files if one is included in this build.
  The hotfixes UPDATE file is included standalone (07_hotfixes_UPDATE_ONLY
  — apply manually once you have the base schema).

VERIFICATION PERFORMED ON THE FINAL FILES (not just assumed correct):
  - Streamed full-file scan (546MB world file) confirming:
      • 257 CREATE TABLE statements = 245 (world base) + 11
        (mod-psychobot world tables) + 1 (battleground_template
        update) — exact arithmetic match, confirming nothing was
        truncated or duplicated.
      • Unescaped single-quote count is even (23,600,794) — strong
        signal against mid-string truncation/corruption across the
        full 546MB file.
      • File ends in a complete, valid statement (not cut off).
  - grep-confirmed zero remaining auth_bfa/characters_bfa/world_bfa
    references and zero DROP DATABASE statements in all 3 files.
  - characters.sql (150 CREATE TABLE) and auth.sql (32 CREATE TABLE)
    passed the same end-of-file and quote-balance checks.

FILES DELIVERED:
  01_psycho_auth.sql        (75 KB)   — complete, ready to import as-is
  02_psycho_characters.sql  (113 KB)  — complete, ready to import as-is
  03_psycho_world.sql       (556 MB)  — complete, ready to import as-is
  Import order: 01 -> 02 -> 03 (world last, since it's independent of
  the other two and is by far the largest).
  Hotfixes DB: still needs its base schema sourced separately — see
  NOTE above. Cannot be marked ready until that schema is provided.
===========================================================================


===========================================================================
 SESSION 9  (2026-06-19)
 World DB delivery — could not download 556MB raw file
===========================================================================

[FIXED — DELIVERY FORMAT] The raw 556MB 03_psycho_world.sql could not be
  downloaded directly (too large for the chat interface). Solved two ways,
  both independently verified byte-perfect against the original:

  1. 03_psycho_world.sql.gz (102 MB) — gzip -9 compressed single file.
     gunzip -t CRC check: PASSED.
     Decompressed content MD5 matches the verified original exactly
     (df1dbcf17839bf0f0469a13b79ce9313).
     Import with: gunzip -c 03_psycho_world.sql.gz | mysql -h 127.0.0.1
       -P 3307 -u psycho -p

  2. 03_psycho_world.part1.sql .. part6.sql (~100MB each, 6 files) —
     split at clean statement boundaries (only ever after a line ending
     in a real ';' statement terminator, never mid-INSERT/mid-string).
     Concatenating all 6 parts back together reproduces the exact same
     MD5 (df1dbcf17839bf0f0469a13b79ce9313) as the original file —
     verified byte-for-byte via `cat part1..part6 | md5sum`.
     Import by running each part in numeric order against psycho_world
     (each part is plain SQL text, no special handling needed beyond
     running them 1 through 6 in sequence).

  Both delivery formats were re-verified again after the raw .sql file
  itself was cleaned up from the workspace (it's large and redundant
  once the .gz/.part files exist) — confirmed both still independently
  decompress/reconstruct to the identical, already-verified content.

  Recommendation: try the .gz first (single file, simplest). Fall back
  to the 6 .part files only if your download path still rejects 102MB.

NO CONTENT CHANGES in this session — purely a delivery-format fix.
All Session 8 findings/fixes stand unchanged.
===========================================================================


===========================================================================
 SESSION 10  (2026-06-19)
 Full re-verification of all 3 databases + final world.sql delivered as .zip
===========================================================================

[CONFIRMED CLEAN] 01_psycho_auth.sql — re-verified from scratch:
  CREATE DATABASE psycho_auth: present. USE statement: present.
  Zero leftover auth_bfa references. Zero DROP DATABASE.
  sql_mode zero-date guard: present. mod-psychobot RBAC section: present.
  File ends on a complete statement. 32 CREATE TABLE statements.
  Quote balance across full file: even (no truncation).

[CONFIRMED CLEAN] 02_psycho_characters.sql — re-verified from scratch:
  CREATE DATABASE psycho_characters: present. USE statement: present.
  Zero leftover characters_bfa references. Zero DROP DATABASE.
  Both update patches confirmed present by content marker
  (2021_12_23_Update_default_inventory, 2022_03_26_00_vulpera_fix_spell).
  All 4 mod-psychobot characters tables confirmed present by content
  marker (ai_playerbot_custom_strategy, ai_playerbot_db_store,
  ai_playerbot_random_bots, psychobot_strategies).
  File ends on a complete statement. 150 CREATE TABLE statements.
  Quote balance across full file: even.

[CONFIRMED CLEAN] 03_psycho_world.sql — re-verified from scratch via TWO
  independent reconstruction paths that had to agree:
    Path A: concatenate the 6 split parts from session 9
    Path B: gunzip the .gz file from session 9
  Both produced the byte-identical file (MD5 df1dbcf17839bf0f0469a13b79ce9313).
  Full streamed re-verification on that reconstructed file confirmed:
    - CREATE DATABASE psycho_world: present. USE statement: present.
    - Zero leftover world_bfa references anywhere in the file.
    - Zero DROP DATABASE statements.
    - sql_mode zero-date guard: both open and close present.
    - 257 CREATE TABLE statements (245 base + 11 mod-psychobot + 1 from
      the battleground_template update) - exact expected count.
    - Quote balance across the full 546M-character file: even
      (23,600,794 unescaped quotes - confirms no mid-statement truncation
      anywhere in the file).
    - All 5 mod-psychobot world SQL files confirmed present by content
      marker (psychobot_cache, psychobot_indexes, psychobot_names,
      psychobot_rpg_races, psychobot_texts).
    - All 29 update files confirmed present by content marker, in the
      correct filename-sorted order matching the core's own DBUpdater.
    - File ends on a complete statement.

[DELIVERY] 03_psycho_world_COMPLETE.zip — single zip archive containing
  the one complete 03_psycho_world.sql file (582,293,227 bytes
  uncompressed inside the zip). Built directly from the just-reverified
  reconstruction (not from an older cached copy). Verified after
  zipping:
    - `unzip -t`: no errors detected in compressed data.
    - Extracted content MD5 matches df1dbcf17839bf0f0469a13b79ce9313
      exactly - identical to every prior verified build of this file.
  Compressed size: 102 MB (from 556 MB raw).

  The earlier session-9 .gz and 6 split-part files were removed after
  the .zip was built and verified, since the .zip now supersedes them
  as the single deliverable for this file.

RESULT OF THIS SESSION: No new defects found. All three database files
re-confirmed structurally and semantically correct from independent
reconstruction, ready for import into MariaDB 11.8.6 at
127.0.0.1:3307 exactly as configured in your worldserver.conf.

Hotfixes DB base-schema gap (see Session 8) remains open and unchanged -
still needs to be sourced separately before 07_hotfixes_UPDATE_ONLY.sql
can be applied.
===========================================================================


===========================================================================
 SESSION 11  (2026-06-19)
 Ground-truth verification: 03_psycho_world_COMPLETE.zip vs ORIGINAL
 uploaded DB-3.zip / DB-4.zip content (not just against my own prior builds)
===========================================================================

This session compared the delivered zip directly against your original
uploaded files for the first time (prior sessions verified internal
consistency between my own builds; this session verifies against your
actual source data).

[VERIFIED] Base world content — streaming line-by-line diff, ALL
  6,350,429 content lines, of the delivered file's base-world section
  against the original DB-4/4_world.sql:
    Method: read both files in lockstep, apply ONLY the two documented
    transformations to the original (CRLF->LF, `world_bfa`->`psycho_world`),
    and require an exact line match at every position.
    Result: 0 real mismatches. (One false "mismatch" was a blank spacer
    line that exists only in a temporary file I created for this
    comparison itself — confirmed by inspection, not part of the
    delivered file.)
  CONCLUSION: every single line of the world base schema/data in the
  delivered zip is byte-for-byte identical to your original DB-4 upload,
  with zero unintended changes of any kind.

[VERIFIED] All 29 world update files — each file's original content
  (after the documented DB-name fix) was confirmed to appear verbatim,
  in full, inside the delivered file. All 29: OK. Includes the 2 files
  containing the hardcoded `world_bfa`.`trainer_spell` reference
  (2022_01_24_world_fix_trainers.sql), confirmed correctly rewritten.

[VERIFIED] All 5 mod-psychobot world SQL files (psychobot_cache.sql,
  psychobot_indexes.sql, psychobot_names.sql, psychobot_rpg_races.sql,
  psychobot_texts.sql) — each confirmed to appear verbatim, in full,
  inside the delivered file.

[INVESTIGATED — NOT A DEFECT] DB-3 vs DB-4 per-table row-count audit.
  Built a correct multi-line-aware row counter (the dump format spans
  each INSERT's VALUES across many lines, one row per line - an earlier
  naive same-line counter in session 8/10 undercounted and was redone
  here) and compared all 216 data-bearing tables between the two
  ORIGINAL uploaded files:
    - 201 tables: identical row counts.
    - 15 tables differ. 14 of those are small (+1 to +228 rows),
      consistent with 3 months of normal upstream development between
      the Jan-2022 (DB-3) and Apr-2022 (DB-4) export dates.
    - 1 table differs significantly: npc_trainer (DB-3: 41,454 rows,
      DB-4: 27,192 rows, -14,262). Traced this to confirm it is NOT
      caused by anything in the 29 update files (those only touch a
      handful of specific trainer/spell rows each via targeted
      DELETE/UPDATE/INSERT - nowhere near 14,262 rows). The drop is
      baked into the DB-4 base snapshot itself: 194 distinct trainer
      IDs present in DB-3 are entirely absent from DB-4, indicating a
      broad upstream trainer-data cleanup that happened directly on the
      source server between the two export dates (most likely removing
      duplicate/decommissioned trainer entries - exactly the kind of
      issue the included "fix_trainers" patches were already chipping
      away at in smaller increments).
  CONCLUSION: this is genuine upstream data evolution between your two
  source exports, not data loss or corruption in either zip or in my
  build. Since DB-4 is the newer, more bug-fixed snapshot, using it as
  the base (the decision made in Session 8) was correct and is now
  further validated, not just for the DROP DATABASE safety reason
  already documented, but because it also carries 3 additional months
  of upstream trainer-data cleanup that DB-3 predates. No action taken
  to merge DB-3's removed rows back in, as doing so would risk
  re-introducing whatever was being cleaned up.

FINAL VERDICT: 03_psycho_world_COMPLETE.zip's content has now been
verified two ways - (1) internal consistency: independently
reconstructed via two different paths (gzip / 6-part split) producing
an identical MD5, and (2) ground truth: every line of base content and
every update/module file traced and matched verbatim back to your
original uploaded files. No discrepancies of any kind remain
unexplained.
===========================================================================


===========================================================================
 SESSION 12  (2026-06-20)
 Ground-truth verification: psycho_auth + psycho_characters vs your
 ORIGINAL uploaded DB-4.zip content (same rigor as Session 11's world check)
===========================================================================

[VERIFIED] 01_psycho_auth.sql base content — full line-by-line diff,
  all 2,080 lines, against the original DB-4/1_auth.sql:
    Method: identical to Session 11 - apply only the documented
    auth_bfa -> psycho_auth substitution + CRLF->LF to the original,
    require an exact line match at every position.
    Result: 0 real mismatches. (The single flagged line was a trailing
    newline difference from my own extraction script, confirmed by byte
    inspection - not a content difference.)
  CONCLUSION: byte-for-byte identical to your original upload, with only
  the documented DB-name substitution applied.

[VERIFIED] 02_psycho_characters.sql base content — full line-by-line
  diff, all 2,858 lines, against the original DB-4/2_characters.sql.
  Result: 0 mismatches of any kind, including trailing-line artifacts.
  CONCLUSION: byte-for-byte identical to your original upload, with only
  the documented characters_bfa -> psycho_characters substitution applied.

[VERIFIED] Both characters/ update files (2021_12_23_Update_default_
  inventory.sql, 2022_03_26_00_vulpera_fix_spell.sql) confirmed verbatim-
  present in the delivered characters file.

[VERIFIED] All 4 mod-psychobot characters-DB files (psychobot_custom_
  strategy.sql, psychobot_db_store.sql, psychobot_random_bots.sql,
  psychobot_strategies.sql) confirmed verbatim-present.

[VERIFIED] mod-psychobot RBAC file (psychobot_rbac.sql) confirmed
  verbatim-present in the delivered auth file.

[VERIFIED] 07_hotfixes_UPDATE_ONLY.sql — confirmed the original
  2022_02_02_hotfixes_fix_fishing_spell.sql content is verbatim-present
  (no DB-name fix was needed in this particular file - it has no
  hardcoded database-qualified table references).

[RE-CONFIRMED] All 4 deliverable files: zero leftover auth_bfa/
  characters_bfa/world_bfa references, zero DROP DATABASE statements,
  all end on complete statements.

FINAL VERDICT (all 4 SQL deliverables, both sessions 11 and 12):
  01_psycho_auth.sql        - ground-truth verified, 100% correct
  02_psycho_characters.sql  - ground-truth verified, 100% correct
  03_psycho_world_COMPLETE.zip (contains 03_psycho_world.sql)
                             - ground-truth verified, 100% correct
  07_hotfixes_UPDATE_ONLY.sql - ground-truth verified, 100% correct
                                 AS A PATCH ONLY (still requires the
                                 missing psycho_hotfixes base schema -
                                 see Session 8 - before it can run)

Every line of base content in every file traces back verbatim to your
original DB-4.zip upload (DB-3.zip correctly excluded per Session 8/11
reasoning). Every update patch and every mod-psychobot SQL file is
verbatim-present in its correct target database file. No discrepancies
of any kind remain unexplained across all 4 deliverables.
===========================================================================


===========================================================================
 SESSION 13  (2026-06-20)
 Completeness audit: does the new DB set cover everything the CORE
 itself actually needs (not just "is the content I gave you correct")
===========================================================================

QUESTION ASKED: "Is everything my core currently needs in the new DBs
you gave me?" — different from prior sessions' "is what you gave me
correct" — this checks for GAPS, by reading the core's own source code
to see exactly what it expects.

[CONFIRMED] Full sql/ directory audit — the core's sql/ tree contains
  ONLY: sql/base/1_auth.sql, sql/base/2_characters.sql, and the 32
  update files (29 world/, 2 characters/, 1 hotfixes/). Nothing else.
  All of these were already incorporated into the 3 consolidated files
  delivered in Sessions 8-12. Nothing in the core's own sql/ tree was
  missed.

[CONFIRMED] Only one module needs SQL — modules/ contains only
  mod-psychobot. No other module directories exist. All 10 of
  mod-psychobot's SQL files (1 auth, 4 characters, 5 world) are
  already folded into the 3 consolidated files.

[CONFIRMED] Found and checked a duplicate copy at
  "Dev/Fixed files/mod-psychobot/sql/" — byte-identical (diff -rq:
  zero differences) to modules/mod-psychobot/sql/, the copy actually
  used. Just a backup from earlier in this project, nothing new.

[CONFIRMED — IMPORTANT CONTEXT] Read the core's own DBUpdater C++
  source (src/server/database/Updater/DBUpdater.cpp) to find out
  exactly what base files the ENGINE ITSELF expects for each database
  when auto-populating an empty DB on first startup:

    Login:     sql/base/auth_database.sql        (LOCATION_REPOSITORY)
    Character: sql/base/characters_database.sql  (LOCATION_REPOSITORY)
    World:     BFADB_837_world_2021_v0.1.sql      (LOCATION_DOWNLOAD)
    Hotfix:    BFADB_837_hotfixes_2021_v0.1.sql   (LOCATION_DOWNLOAD)

  This explains and CONFIRMS several things:
    1. World DB is NOT bundled in the core's repo by design - it's
       meant to be downloaded separately as "BFADB_837_world_2021_v0.1.sql".
       Your DB-3.zip/DB-4.zip 4_world.sql files ARE that download
       (just renamed) - confirming the entire Session 8 approach
       (building 03_psycho_world.sql from DB-4) was exactly correct,
       and my version is actually MORE complete than that raw download
       alone would be, since all 29 updates + mod-psychobot tables are
       already pre-applied.
    2. Hotfix DB is likewise NOT bundled - same LOCATION_DOWNLOAD
       mechanism, expecting a file literally named
       "BFADB_837_hotfixes_2021_v0.1.sql" placed in the worldserver's
       working directory. THIS IS THE SPECIFIC FILE YOU NEED TO SOURCE
       to close the hotfixes gap from Session 8 - it should exist
       wherever your "BFADB_837_world_2021_v0.1.sql" (i.e. your DB-3/
       DB-4 zips) originally came from, as a sibling release file.
    3. [MINOR, NOT A BLOCKER] Login's and Character's own auto-populate
       paths reference filenames (auth_database.sql,
       characters_database.sql) that DON'T match the actual files
       present (1_auth.sql, 2_characters.sql). This is a pre-existing
       naming mismatch in the core itself. It is irrelevant to your
       situation: DBUpdater::Populate() only ever runs this lookup when
       a database is completely empty (SHOW TABLES returns 0 rows). You
       are importing my pre-built 01_psycho_auth.sql /
       02_psycho_characters.sql files manually via mysql client, so the
       databases will already have tables before the worldserver ever
       starts, and this code path never triggers. Flagged for
       awareness only.

[CONFIRMED] dep/mysql/create_psycho_databases.sql — found and reviewed.
  This is a bootstrap script that ONLY creates the 4 empty databases
  (psycho_auth, psycho_characters, psycho_world, psycho_hotfixes) and
  the `psycho` MySQL user with grants - it contains NO table schemas of
  any kind. Confirms the 4 database names used throughout all my
  deliverables are exactly correct. Does not affect or close the
  hotfixes gap (no schema in this file either).
  SIDE NOTE (not a SQL-content issue): this bootstrap script sets the
  psycho user's password to 'core', not 'psycho' as in the connection
  strings you provided (...;psycho;psycho;psycho_auth implies
  password=psycho). Worth a quick check on your end as to which is
  actually active on your server - unrelated to the SQL files
  themselves, just a credentials note.

===========================================================================
 FINAL ANSWER: does the new DB set cover everything the core needs?
===========================================================================
  YES, with one specific, named exception:

  COVERED (100%, ground-truth verified across Sessions 8-13):
    - psycho_auth        - complete, matches everything sql/base/
                            1_auth.sql + mod-psychobot RBAC require
    - psycho_characters   - complete, matches everything sql/base/
                            2_characters.sql + both update patches +
                            all 4 mod-psychobot tables require
    - psycho_world        - complete, matches everything the original
                            world dump + all 29 update patches + all 5
                            mod-psychobot world files require. This
                            database was NEVER part of the core's own
                            repo by design (LOCATION_DOWNLOAD) - your
                            DB-4 upload was the only correct source for
                            it, and it's now fully incorporated.

  NOT COVERED (genuine gap, cannot be fixed from anything provided):
    - psycho_hotfixes base schema - needs a file matching
      "BFADB_837_hotfixes_2021_v0.1.sql" (or equivalent content) from
      the same release source as your world database download. Neither
      DB-3.zip, DB-4.zip, the core's own repo, nor the mysql bootstrap
      script contain this. This is the ONLY remaining gap between what
      you have and what the core needs to fully start.
===========================================================================


===========================================================================
 SESSION 14  (2026-06-20)
 Auto-updater configuration guidance — separate small report issued
===========================================================================

User has modified the core's auto-updater (DBUpdater.cpp) since the last
upload and wants it to look for/load psycho_auth, psycho_characters,
psycho_world, psycho_hotfixes directly. Re-confirmed all 3 existing SQL
files are still structurally intact (CREATE DATABASE lines present,
files end cleanly, world.zip passes integrity test) - no changes made to
their content this session.

Produced a separate, focused deliverable: Updater_Config_Guide.txt,
covering:
  - Where GetBaseFile() needs to point for each of the 4 database types
    (based on the LAST KNOWN version of DBUpdater.cpp - user's current
    modified version not yet seen/verified)
  - GetBaseLocationType() should change from LOCATION_DOWNLOAD to
    LOCATION_REPOSITORY for World/Hotfix now that they're bundled locally
  - CRITICAL gotcha flagged: sql/updates/world/ and sql/updates/
    characters/ still contain the same 31 files already merged into my
    consolidated base SQLs. Left in place, the updater will try to
    re-apply them on top of data that already has them, causing
    duplicate-key errors. Two fix options given (move/delete the update
    files, or pre-populate the `updates` tracking table).
  - hotfixes gap remains open and explicitly called out as the one
    startup error that will remain even after Steps 1-3 are done.

NOTE: this guidance is based on DBUpdater.cpp as last analyzed (Session
13), since the user's current modified version has not been uploaded.
Flagged clearly in the new report; recommend the user share the current
DBUpdater.cpp if they want it checked against their actual changes
rather than the prior baseline.
===========================================================================
